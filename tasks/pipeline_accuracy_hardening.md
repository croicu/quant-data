# Pipeline Accuracy Hardening

Slice 1 (schema-only: `dim_field`, `provider_pair_disagreement` key, `dataset_inception`) tracked
at [croicu/quant-data#28](https://github.com/croicu/quant-data/issues/28), which also carries
forward the full converged design for slice 2 (algorithm + CLI), to be opened as its own issue
once slice 1 lands.
